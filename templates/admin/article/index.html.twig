{% extends 'base.html.twig' %}

{% block title %}
    Gestion des articles
{% endblock %}

{% block body %}

    <h1 class="text-center">Gestion des articles ({{ articles|length }})</h1>

    <a href="{{ path('admin_article_add') }}" class="btn btn-outline-primary mb-3">
        Nouvel article
    </a>

    <table class="table">
        <thead>
            <tr class="table-dark">
                <th>ID</th>
                <th>Catégorie</th>
                <th>Titre</th>
                <th>Date de publication</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ article.id }}</td>
                <td>{{ article.category.name }}</td>
                <td>{{ article.title }}</td>
                <td>
                    {% if article.publishedAt is not null %}
                        {{ article.publishedAt|date('d/m/Y') }}
                    {% endif %}
                </td>
                <td>
                    <a href="{{ path('admin_article_edit', {'id': article.id}) }}" class="btn btn-outline-primary">
                        Modifier
                    </a>
                    <a href="#" class="btn btn-outline-danger">
                        Supprimer
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
